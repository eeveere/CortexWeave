# Checkpoint 32: Context Explainability

## Completed

- Added opt-in per-item context explanations for semantic and resume packets.
- Each explanation reports stable source identity, selection reasons, component
  scores, token estimate, and truncation state without repeating prompt content.
- Added CLI `--explain` and MCP `include_explanation` request options.
- Ensured explanation generation runs only after selection and remains outside
  the packet token budget.

## Verification

The focused explanation test and the full formatter, check, Clippy, and test
gate pass.
