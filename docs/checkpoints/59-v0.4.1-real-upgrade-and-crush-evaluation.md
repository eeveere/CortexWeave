# v0.4.1 Real Upgrade and Client Evaluation

**Date:** 2026-08-29  
**Scope:** Breakpoint 2 adversarial qualification on Sol/xhigh. Acceptance did
not pass; Final Hardening begins only after the listed blockers are fixed.

## Isolation and Source Identity

The evaluation used an online SQLite backup and a copied OPiHype working tree.
The original operational database and repository were not mutated. The source
checkout was at commit `a0b5156abaf87f5430d44b1262f4527dac67fb30` with
existing tracked and untracked working-tree changes; the copy preserved that
working-tree state.

The operational database was the registered `opihype` workspace, not the
unrelated empty project-local schema-v3 database. Before current-binary open it
contained successful migrations 1–5 and reported:

| Observation | Before repair |
|---|---:|
| Documents / chunks / embeddings | 149 / 18,610 / 18,610 |
| Embedding space | `nomic-embed-text-v1.5@fb0dae47281fe6b7`, 768 dimensions |
| Content / graph revision | 2 / 2 |
| Graph state | current |
| Nodes / edges / unresolved | 1,409 / 2,928 / 974 |
| Python / generic-text documents | 58 / 91 |
| Database bytes | 517,595,136 |

The current binary migrated the copy through migration 6 without changing the
source or graph revision and populated 149 projection manifests.

## Repair Evidence

The first isolated copy deliberately omitted repository and client metadata.
`if_needed` failed closed with `source_drift`, zero graph writes, and zero
embeddings. Investigation proved the operational index contained 27 `.git`
files and `.opencode/codebase-index.json`. Restoring those copied paths made all
149 persisted source hashes match.

That failure exposed a recovery contradiction: the durable generation remained
`failed`, but a later `if_needed` request returned `reason: current`, created no
replacement generation, and graph status reported `is_current: true`.
Structural reads still correctly rejected the failed generation. A forced
repair was required to recover.

The forced repair completed twice with the same bounded result:

| Observation | Result |
|---|---:|
| Documents considered / analyzed / failed | 149 / 149 / 0 |
| Nodes / resolved edges / unresolved | 1,409 / 2,996 / 906 |
| Embeddings computed | 0 |
| Source revision changed | false |
| Content / graph revision after | 2 / 2 |
| Elapsed time, release build | 30.76 s |
| Sampled peak working set | at least 56,401,920 bytes |

A following `if_needed` run was an explicit current/no-op with zero analyzed
documents, zero graph changes, and no generation. The 68-edge convergence delta
from the prior current graph is retained as evidence from full reprojection; it
is not presented as a source or embedding change.

Preservation digests were identical before and after repair:

```text
chunk IDs SHA-256:
6d4fb01eb9fd27758857965b405b3af238a4df9d7a5e02e977cd835846dedd6a

embedding rows SHA-256:
8c1d9093a7f15d46f4add21b16a6263258fba29700ed58643596b0223f82b558

document revision/content SHA-256:
0d0c4a3a6a886074da425ef6d5f287d45480c3e71eebcafb7a682e1251cc0f51
```

The main database file remained 517,595,136 bytes; SQLite created a 32,768-byte
shared-memory sidecar and left no pending WAL bytes after the run.

## Structural and Context Evidence

Exact lookup succeeded for `MemoryWatchdog._restart` at
`backend/memory_watchdog.py` bytes 7,731–9,789 with node ID
`f8a728bd-f261-4582-8e87-914d7cb34325`. Direct callers, callees, dependencies,
implementations, and likely tests all returned empty bounded results. The source
contains dynamic member calls, including `self.process_manager.restart()`, but
the conservative Python analyzer intentionally does not invent member-dispatch
edges.

With the live configured embedding endpoint, the active-job restart context
query returned current provenance including:

- `MemoryWatchdog._begin_restart` in `backend/memory_watchdog.py`;
- `_wait_for_watchdog` in `backend/job_runner.py`;
- `ProcessManager.restart` in `backend/process_manager.py`;
- watchdog lifecycle material in `tests/test_core.py`.

Every selected item's `structural_evidence` was empty. Some items nevertheless
received structural scores from logical chunk containment/neighbor expansion.
This is the plan's explicit “graph tools succeeded while context omitted graph
evidence” failure classification. D068 accepts a bounded zero-distance graph
seed as the remedy; it does not authorize inferred call edges.

A fresh MCP stdio process proved the absolute root hint selected only the copied
OPiHype workspace and returned the current graph. Its initialize response also
reported server version `0.1.0`. No installed or launchable Crush executable or
Windows app was available, so actual Crush compliance remains unqualified. The
user will run the three fresh Crush experiments after local blockers pass.

## Final-Hardening Blockers

1. **Repair recovery/status:** incomplete active, failed, or interrupted repair
   state must override an otherwise current graph watermark in planning and
   `is_current` diagnostics. `if_needed` must replace/retry the failed
   generation and converge.
2. **Workspace discovery:** exclude `.git` entries deterministically while
   retaining deliberate hidden project files. Add a regression proving mutable
   Git internals never become documents.
3. **Release identity:** set Cargo package and lockfile identity to `0.4.1` and
   assert packaged binary and MCP server versions.
4. **Graph-aware context:** include bounded exact-seed graph evidence under
   D068, preserve revision/limits/source provenance, and keep unproven direct
   relationship results empty.

After these fixes, rerun the copied upgrade, force/no-op preservation checks,
direct structural experiment, and graph-aware context query. Only then ask the
user for the three fresh Crush transcripts.
