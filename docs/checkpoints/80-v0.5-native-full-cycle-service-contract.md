# v0.5 Checkpoint 80: Native Full-Cycle Service Contract

**Date:** 2026-08-31

**Scope:** Phase 9 on GPT-5.6 Terra/high

## Result

A new direct integration test drives the complete Experience path through
`CortexWeaveService` alone. It creates a workspace, initial session/task,
explicit debugging episode, and five typed events: failure, change, failed
verification, change, and passing verification. It associates and closes the
episode, previews and accepts the automatic consolidation, and verifies the
two-attempt successful Experience through detail inspection.

The test then ends the initial session and starts a fresh session and task with
no prior transcript. A newly recorded matching compiler failure activates
current Event context. Normal Experience search finds the persisted active
Experience, while context includes one bounded historical Experience only after
the fresh task state and current failure event. The packet keeps workspace,
session, and task provenance and excludes every initial-session event.

Finally, repeating the same acceptance returns the original Experience ID.
The test contains no MCP, CLI, JSON-RPC, stdio, model API, or runtime harness
dependency.

## Verification

The focused native full-cycle integration test passed. The next breakpoint
remains unstarted.
