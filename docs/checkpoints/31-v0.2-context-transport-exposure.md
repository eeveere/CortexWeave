# Checkpoint 31: Context Transport Exposure

## Completed

- Exposed `resume_context` through the CLI and MCP adapter.
- Added CLI and MCP operations for working-set inspection, pin and unpin, and
  checkpoint creation and latest-checkpoint retrieval.
- Kept every handler thin: adapters resolve input and call the application
  facade; selection, scoring, and budgeting remain in `ContextService`.
- Documented the commands, MCP tools, selection envelope, and MCP budget cap.
- Added MCP tool-call coverage and CLI resume coverage.

## Verification

`cargo fmt --all -- --check`, `cargo check --all-targets --all-features`,
`cargo clippy --all-targets --all-features -- -D warnings`, and
`cargo test --all-targets --all-features` all pass.
