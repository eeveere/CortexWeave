# Checkpoint 40: v0.3 Implementation Coverage

## Completed

- Added named implementation-coverage cases to final-packet evaluation.
- Each expectation declares an exact source path and normalized symbol.
- Only matching code items satisfy coverage; selected plan or configuration
  mentions are reported separately.
- Aggregate results include per-case coverage, pass state, suite pass rate, and
  mean coverage without changing the existing packet metrics.

## Acceptance Scenarios

The OPiHype-derived integration fixture covers:

- llama-server lifecycle evidence for `ProcessManager.ensure_ready`,
  `ProcessManager.restart`, `MemoryWatchdog`, and `lifespan`;
- watchdog restart during an active run for `MemoryWatchdog._begin_restart`,
  `MemoryWatchdog._restart`, `JobRunner._wait_for_watchdog`, and the lifecycle
  test.

Both bounded packets achieve complete implementation coverage while a planning
document that names the same symbols remains mention-only evidence.

## Boundary

The evaluator inspects completed `ContextPacket` values. It does not retrieve,
rank, expand, or alter packet contents and remains independent of CLI and MCP
types.
